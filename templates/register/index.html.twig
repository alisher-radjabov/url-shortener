{% extends 'base.html.twig' %}

{% block title %} Create user {% endblock %}

{% block body %}
    
    {{ include('inc/navbar.html.twig') }}
    
    <div class="container">

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
        
    </div>

{% endblock %}
